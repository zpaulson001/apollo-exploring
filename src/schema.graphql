type Query {
  pokemon(id: ID!): Pokemon!
  allTrainers: [Trainer!]!
}

type Trainer {
  id: ID!
  name: String!
  pokemon: [Pokemon!]
}

type Pokemon {
  id: ID!
  name: String!
  types: [String!]!
  height: Int!
  weight: Int!
}
